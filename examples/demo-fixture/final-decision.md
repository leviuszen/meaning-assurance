# Final Decision

**State:** `BLOCKED`

The worker's `COMPLETE` claim is not accepted.

Required before reconsideration:

1. run and record the missing default-behavior check;
2. revert or separately authorize the out-of-scope documentation change; and
3. provide a fresh frozen evidence packet.

The rejected and not-testable reviewer findings do not create additional
requirements.

Final acceptance remains with the human decision maker.
